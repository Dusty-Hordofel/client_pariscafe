import { createBrowserHistory } from 'history';

//forceRefresh: true is used to force the browser to refresh the page when the user clicks the back button
export default createBrowserHistory({ forceRefresh: true });
